template:
  type: text
  template: |
    You are a SPARQL expert. The following SPARQL query failed. Your task is to fix it.
    Use the provided schema, the original question, the faulty query, and the error message.

    Ontology Schema:
    {schema}

    Original Question:
    {question}

    Faulty SPARQL Query:
    {sparql_query}

    Error Message:
    {error_message}

    Corrected SPARQL Query (provide only the raw query):
